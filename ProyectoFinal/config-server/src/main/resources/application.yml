spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ricardohuaripata/TodoCode-Curso-Microservicios.git
          searchPaths: ProyectoFinal/config-data
  security:
    user:
      name: root
      password: s3cr3t

server:
  port: 8081
